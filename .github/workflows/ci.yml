name: CI
on:
  workflow_dispatch:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  foundry-ci:
    uses: sarthakagrawal927/saas-maker/.github/workflows/foundry-ci.yml@main
